# v0.22.0 Release Manifest

## Release

- Version: 0.22.0
- Status: Beta
- Commit: 4f11a60 ("release: v0.22.0 beta", branch main)
- Tag: v0.22.0 (annotated, points at 4f11a60)

## Included Components

- `plugin.yaml` (0.22.0), `README.md` (0.22.0 + durability notes + install)
- `setup.sh`, `uninstall.sh` (mode 100755 in index)
- `src/federated_db.py` (event buffer, recovery, idempotent persistence, health)
- `tests/test_v022_*.py` (9 suites) + `tests/crash_child.py` (driver)
- `results/v0.22_architecture.md`, `v0.22_durability_validation.md`,
  `v0.22_recovery_matrix.md`, `v0.22_final_gap_audit.md`,
  `v0.22_release_checklist.md`, `v0.22_release_notes.md`,
  `v0.22_installer_validation.md`
- Excluded deliberately: pre-existing `src/__init__.py` /
  `src/enhancer.py` import edits, root stray files, v0.21 audit doc

## Validation

- Executed now: 51/51 (installer 8, crash 4, startup 5, malformed 6,
  corruption 6, async 4, failure 6, storage 3, pipeline 9)
- Previously validated (final code, unchanged since): stress 5/5
  (10K/10K/6000 zero-loss-zero-dupe), multiprocess 1/1, combined 49/49
- Broader suite: 46 passed + 2 pre-existing env failures (missing
  `~/.hermes/plugins` path; resolved to 5/5 once the plugin is installed)

## Installer

- `setup.sh`: validated (8/8 incl. live real-home install, discovery PASS)
- `uninstall.sh`: validated (scope + double-run safe)

## Durability

«No duplicate persisted event_id and no loss of confirmed durable
buffered events across tested normal process crash/restart scenarios.»

## Non-Guarantees

- Power-loss durability not validated.
- Physical disk-full not physically induced.
- Telemetry counters are process-local.
- No universal exactly-once guarantee.

## Release Integrity

- Historical artifacts untouched (baselines byte-identical in git).
- No secrets found in released files.
- `~/.hermes/state.db` untouched (never referenced; installer has no
  state.db/sqlite/doctor access in executable lines).
- Tag v0.22.0 verified pointing at release commit 4f11a60.
- Remote push NOT performed (no credentials/GH CLI); see below.

## Publication (manual)

Remaining commands for an authorized environment:

    git push origin main
    git push origin v0.22.0
    gh release create v0.22.0 --title "Hermes Enhancer v0.22.0 Beta" \
      --prerelease --notes-file results/v0.22_release_notes.md
